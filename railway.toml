[build]
builder = "RAILPACK"
buildCommand = "npm run build"
watchPatterns = [
  "src/**",
  "package.json",
  "package-lock.json",
  "railway.toml",
  "tsconfig.json",
  ".npmrc",
]

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 100
